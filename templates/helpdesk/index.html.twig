{% extends "base.html.twig" %}

{% block body %}
    {% if data.integrationName is defined %}
        Loading orders from:
        {{ data.integrationName }}
    {% else %}
        Sorry but there is no such integration!
    {% endif %}
{% endblock %}

